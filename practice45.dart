void main(){
    List<int>numbers=[1,2,3,4,5,6,7,8];
    print(' the elements of lists is = $numbers');

    int length=numbers.length;
     print(' the length of list is = $length');
    print(' the length of list is = ${numbers.length}');    //another way to check length od list

    bool isEmpty=numbers.isEmpty;                //is empty property
    print('$isEmpty');
    

}